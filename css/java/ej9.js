const person = {
    firstName: "Mario",
    lastName: "Rossi",
    age: 25
}

//console.log(person);

let keys = Object.keys(person)

console.log(keys);

let values = Object.values(person)

console.log(values);


let entries = Object.entries(person)

console.log(entries);